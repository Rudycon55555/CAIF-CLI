# HOWtoUSEartifact.md

## How to Use an Exported Artifact (AI Model)

An **artifact** in CAIF‑CLI is the exported `.py` file created by the `EXPORT MODEL` command.  
It contains learned weights, a `predict()` function, and optional XP/TC connection logic.

---

### 📦 Running the Artifact

After training and export, you’ll have a Python (PY) file to use!
